# Codex exact-head receipt probe

This inert file exists only to validate the automatic Codex review receipt on
this pull request's exact head. It changes no runtime, deployment, credential,
or production behavior.

# Public local CLI receipt canary: validates the updated exact-head control plane.
