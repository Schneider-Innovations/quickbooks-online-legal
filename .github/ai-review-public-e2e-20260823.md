# Codex public-repository receipt probe

This inert file verifies the local exact-head receipt workflow required for a
public repository. It changes no runtime, deployment, credential, or
production behavior.

